project_name: secman

release:
  prerelease: auto
  draft: true
  name_template: "Secman {{ .Version }}"

before:
  hooks:
    - go mod tidy

builds:
  - <<: &build_defaults
      binary: bin/secman
      main: ./core
      ldflags:
        - -X main.version={{ .Version }}
        - -X main.versionDate=(31-05-2021)

    id: macos
    goos: [ darwin ]
    goarch: [ amd64 ]

  - <<: *build_defaults
    id: linux
    goos: [ linux ]
    goarch: [ amd64, arm64 ]
    env:
      - CGO_ENABLED=0

  - <<: *build_defaults
    id: windows
    goos: [ windows ]
    goarch: [ amd64 ]

archives:
  - id: nix
    builds: [ macos, linux ]
    <<: &archive_defaults
      name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Version }}_{{ .Arch }}{{ if .Arm }}{{ .Arm }}{{ end }}"

    wrap_in_directory: true
    replacements:
      darwin: macos
    format: zip
    files:
      - LICENSE
      - ./share/man/man1/secman.1

  - id: windows
    builds: [ windows ]
    <<: *archive_defaults
    wrap_in_directory: false
    format: zip
    files:
      - LICENSE

nfpms:
  - license: MIT
    maintainer: Secman
    homepage: https://github.com/secman-team/secman
    bindir: /usr
    file_name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Arch }}"
    dependencies:
      - git
    description: The Password Manager of your Dreams.
    formats:
      - deb
      - rpm
    files:
      "./share/man/man1/secman.1": "/usr/share/man/man1"
